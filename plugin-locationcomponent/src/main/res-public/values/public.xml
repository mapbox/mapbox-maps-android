<?xml version="1.0" encoding="utf-8"?>
<!-- This file is generated. -->
<resources>

    <!-- Whether the user location is visible on the map. -->
    <public name="mapbox_locationComponentEnabled" type="attr" />

    <!-- The stale state indicates to the user that the location being displayed on the map hasn't been updated in a specific amount of time. -->
    <public name="mapbox_locationComponentStaleStateEnabled" type="attr" />

    <!-- Set the delay before the location puck becomes stale. The timer begins approximately when a new location update comes in and using this defined time, if an update hasn't occured by the end, the location is considered stale. -->
    <public name="mapbox_locationComponentStaleStateTimeout" type="attr" />

    <!-- The scale factor of the location icon when the map is zoomed out. Scaling is linear. -->
    <public name="mapbox_locationComponentMinZoomIconScale" type="attr" />

    <!-- The scale factor of the location icon when the map is zoomed in. Scaling is linear. -->
    <public name="mapbox_locationComponentMaxZoomIconScale" type="attr" />

    <!-- Sets the id of the layer that's added above to when placing the component on the map. -->
    <public name="mapbox_locationComponentLayerAbove" type="attr" />

    <!-- Sets the id of the layer that's added below to when placing the component on the map. -->
    <public name="mapbox_locationComponentLayerBelow" type="attr" />

    <!-- Location puck definitions for two_d_location_puck.  -->

    <!-- Name of image in sprite to use as the top of the location indicator. -->
    <public name="mapbox_locationComponentLocationPuckTwoDLocationPuckTopImage" type="attr" />
    <!-- Name of image in sprite to use as the middle of the location indicator. -->
    <public name="mapbox_locationComponentLocationPuckTwoDLocationPuckBearingImage" type="attr" />
    <!-- Name of image in sprite to use as the background of the location indicator. -->
    <public name="mapbox_locationComponentLocationPuckTwoDLocationPuckShadowImage" type="attr" />
    <!-- Defines the bearing color. -->
    <public name="mapbox_locationComponentLocationPuckTwoDLocationPuckBearingTintColor" type="attr" />
    <!-- Defines the bearing color. -->
    <public name="mapbox_locationComponentLocationPuckTwoDLocationPuckBearingStaleTintColor" type="attr" />
    <!-- Defines the foreground color -->
    <public name="mapbox_locationComponentLocationPuckTwoDLocationPuckTopTintColor" type="attr" />
    <!-- Defines the foreground stale color. -->
    <public name="mapbox_locationComponentLocationPuckTwoDLocationPuckTopStaleTintColor" type="attr" />
    <!-- Defines the background color -->
    <public name="mapbox_locationComponentLocationPuckTwoDLocationPuckShadowTintColor" type="attr" />
    <!-- Defines the background stale color. -->
    <public name="mapbox_locationComponentLocationPuckTwoDLocationPuckShadowStaleTintColor" type="attr" />
    <!-- Whether the location puck is pulsing on the map. -->
    <public name="mapbox_locationComponentLocationPuckTwoDLocationPuckPulsingEnabled" type="attr" />
    <!-- The color of the pulsing circle. -->
    <public name="mapbox_locationComponentLocationPuckTwoDLocationPuckPulsingColor" type="attr" />
    <!-- The maximum radius of the pulsing circle. -->
    <public name="mapbox_locationComponentLocationPuckTwoDLocationPuckPulsingMaxRadius" type="attr" />

    <!-- Location puck definitions for three_d_location_puck.  -->

    <!-- An URL for the model file in gltf format. -->
    <public name="mapbox_locationComponentLocationPuckThreeDLocationPuckModelUri" type="attr" />
    <!-- The scale of the model. -->
    <public name="mapbox_locationComponentLocationPuckThreeDLocationPuckPosition_lon" type="attr" />
    <public name="mapbox_locationComponentLocationPuckThreeDLocationPuckPosition_lat" type="attr" />
    <!-- The opacity of the model. -->
    <public name="mapbox_locationComponentLocationPuckThreeDLocationPuckModelOpacity" type="attr" />
    <!-- The scale of the model. -->
    <public name="mapbox_locationComponentLocationPuckThreeDLocationPuckModelScale_x" type="attr" />
    <public name="mapbox_locationComponentLocationPuckThreeDLocationPuckModelScale_y" type="attr" />
    <public name="mapbox_locationComponentLocationPuckThreeDLocationPuckModelScale_z" type="attr" />
    <!-- The rotation of the model. -->
    <public name="mapbox_locationComponentLocationPuckThreeDLocationPuckModelRotation_x" type="attr" />
    <public name="mapbox_locationComponentLocationPuckThreeDLocationPuckModelRotation_y" type="attr" />
    <public name="mapbox_locationComponentLocationPuckThreeDLocationPuckModelRotation_z" type="attr" />

    <!-- Defines the preset puck styles bundled with the plugin. -->
    <public name="mapbox_locationComponentPresetPuckStyle" type="attr" />
</resources>
<!-- End of generated file. -->